let initialState = {
    eventsData: {text: "В настоящее время вы просматриваете свою общедоступную страницу, это то, что увидят другие пользователи."}
}

const eventsReducer = (state = initialState, action) => {
    return state;
}

export default eventsReducer;